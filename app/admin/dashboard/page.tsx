import AdminDashboard from '@/components/platform-admin/AdminDashboard';

export const metadata = { title: 'Velaro — Platform Dashboard' };

export default function AdminDashboardPage() {
  return <AdminDashboard />;
}
